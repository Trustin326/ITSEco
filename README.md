# ItsEco — Harb Ventures CFO Command Center (Web Demo)

Ready-to-push Next.js static demo configured for **GitHub Pages** under repo name **ItsEco**.

## Local
npm install
npm run dev

## Export (static for Pages)
npm run export   # creates ./out

## Deploy via GitHub Action (auto)
- Commit this folder to `main`.
- Enable Pages in Settings after first run.
- The workflow `.github/workflows/pages.yml` builds and publishes `out/` to GitHub Pages.

Live URL: https://<your-username>.github.io/ItsEco/
